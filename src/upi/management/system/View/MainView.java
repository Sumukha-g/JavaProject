package upi.management.system.View;

import javax.swing.*;
import java.awt.*;

public class MainView extends JFrame {
    public MainView(String pin) {
        super("Select Your Transaction");
        setLayout(null);
        setSize(1550, 1080);
        setLocation(0, 0);
    }
}